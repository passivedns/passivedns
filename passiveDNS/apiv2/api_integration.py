from fastapi import APIRouter

api_integration_router = APIRouter()

# post /apiintegration/{nom} + depends User - get request avec objet IP ou DomainName
# create object
# make request 
# update ip or domain with new data
# return data 